# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*

# Editor/OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Office lock files
~$*

# Internal planning notes (not part of the site)
Hoja de ruta.docx

# Local Claude Code settings and skills (internal tooling, not the public site)
/.claude/

# Internal business tooling — quote calculator, pricing formulas, generated
# PDFs (may contain customer PII). Kept locally only, never published.
/quotes/

# Standalone backup copy of the site and its root-level assets — not used
# by the deployed build (GitHub Pages only builds from cleaning-site/).
# Kept locally as a personal reference/backup.
/pristine.html
/logo-export.html
/Logo.PNG
/santiago.jpg
/favicon.svg

# Project instructions for Claude Code (not part of the public site)
/CLAUDE.md
